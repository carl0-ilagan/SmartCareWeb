rules_version = '2';

// Note: Firebase Storage is not used - all images are stored as base64 in Firestore
// These rules are kept for reference but won't be used
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
